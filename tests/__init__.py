"""
Cpank Test Suite

Using Environment variable 'CPANK_EXE', path to pankti executable can set
cpank build is recommended to be without `DEBUG` flag, basically it must not 
print `~p ` on show/print statements (default behavior on DEBUG builds). 
If you must you can set `CPANK_DEBUG` Environment variable to `1` or `t` 
or `true`
"""
