import h "map"

let a = {"name" : "palash" , "age" : 21}
show a["name"]
a["title"] = "bauri"
show a
show h.iskey(a , "1")
let x = h.keys(a)
show x[2]
show len(x)
let y = h.values(a)
show y
show type(y[0])
show len(y)
